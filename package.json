{
  "name": "skillstate-proxy",
  "version": "0.1.0",
  "description": "LLM agent token savings proxy — drops prompt tokens 60%+ on long-horizon tasks via SKILL.state (arXiv:2608.26263). OpenAI-compatible, works with any upstream.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "skillstate": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "llm",
    "agent",
    "skillstate",
    "token-savings",
    "context-management",
    "long-horizon",
    "proxy",
    "openai-compatible",
    "anthropic",
    "cost-optimization",
    "prompt-compression",
    "arxiv"
  ],
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
